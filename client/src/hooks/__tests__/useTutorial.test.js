import { describe, it, expect } from 'vitest';

// Placeholder JS-only test to avoid JSX parsing in .js; see .jsx sibling for real tests
describe('useTutorial (placeholder)', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
// (Real tests live in useTutorial.test.jsx)
